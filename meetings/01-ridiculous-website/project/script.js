// BUILD LAB EDIT 5 (optional): Change the secret message or add another
// action after the baseline HTML and CSS edits are working.
onClick("surprise-button", function () {
  show("surprise-message");
});
