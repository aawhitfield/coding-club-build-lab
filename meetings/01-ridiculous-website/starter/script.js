// The first path needs no JavaScript. This small Boss Level is already wired
// so beginners can change the message before they study the event.
onClick("surprise-button", function () {
  show("surprise-message");
});
