# Recipe Guide

Choose one recipe only until it works.

1. Copy the HTML elements it needs.
2. Copy the JavaScript pattern.
3. Make the IDs match exactly.
4. Change the sample data.
5. Run a normal case and an edge case.
6. Record the test in `TEAM-LOG.md`.

Do not add a second recipe just because the first one is not working. Debug first.
